---
import DocLayout from '@/layouts/DocLayout.astro';
---

<DocLayout title="polemil.dev — design system" page="foundations-colors">
  <section style="padding: var(--space-8); max-width: var(--size-center-max); margin: 0 auto;">
    <p style="font-family: var(--font-mono); font-size: var(--text-2xs); text-transform: uppercase; letter-spacing: 0.05em; color: var(--fg-tertiary);">
      Scaffold — phase 1 tickets pending
    </p>
    <h1 style="font-size: var(--text-2xl); margin-block-start: var(--space-2);">
      polemil.dev
    </h1>
    <p style="color: var(--fg-secondary); margin-block-start: var(--space-3);">
      The squelette is in place. Components, patterns and pages will be filled in via the GitHub backlog (étape 5).
    </p>
  </section>
</DocLayout>
